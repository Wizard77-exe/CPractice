#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <string.h>
#include <ctype.h>

// Singly Linked List Structure
typedef struct {
    int data;
    SNode *next;
} SNode;

// doubly Linked List Structure
typedef struct {
    int data; 
    DNode *next;
    DNode *prev;
} DNode;

typedef struct {
    
} Vector;

int main() {

    return 0;
}